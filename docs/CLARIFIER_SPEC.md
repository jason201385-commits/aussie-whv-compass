# 澳打指南針 — 首頁單一漏斗釐清器規格（CLARIFIER_SPEC，P0-7）

> 版本 0.1｜最後更新 2026-09-02｜狀態：**決策已定／設計未開始**。
> §1–§2 是站長 2026-09-02 拍板的前提與邊界（`DECISIONS.md` D-2026-09-02-01），實作者不得再問。
> §3–§7 是主 session 依決策整理的**設計骨架草案**，站長審核前不得動工；審核後把本檔版本升到 1.0
> 並在 `ROADMAP.md` 把 P0-7 狀態改為「未開始」。閱讀順序：`SDD.md` §1.1 → `SPEC.md` §0 → 本檔。

## 0. 目標與成功定義

- 目標（站長原話）：使用者進站能快速釐清自己的澳打問題並找到資訊；「板面太豐富，文字太多反而失焦」；
  「最終目的是讓活人可以彼此連結」。
- 成功定義（沿用 SPEC §1.5 自願任務測試）：新訪客 30 秒內找到正確情境與第一個安全下一步；
  釐清器完成率；AI 兜底被觸發的比例應低（點選路徑優先）。實際數字要等量測上線後才有基線。

## 1. 前提決策（Accepted，來源 D-2026-09-02-01）

| ID | 範圍 | 必須 | 不得 | 資料流 | 非目標 | 未決 gate |
|---|---|---|---|---|---|---|
| C-1 首頁重建 | `index.html` | 重建為單一漏斗；每個既有區塊都有去向（§3.2） | 不受 PERF spec「零數據不刪功能」限制，但不得無聲丟掉功能 | 無新資料流 | 不改內容頁 | 站長審核 §3 |
| C-2 優先族群 | 漏斗第一層 | 第一：出發前的台灣人與中國護照（462）；第二：已在澳洲的人 | 不做第三族群的專屬路徑 | 護照選擇只在當頁記憶體 | 簡中 462 專頁列第二階段 | 無 |
| C-3 462／簡中 | 護照分流 | 第一階段只分護照並連到 `lang/en/visa/` 既有 462 內容 | 不生成簡中整頁 | 無 | 簡中 462 專頁 | 第二階段另開 ID |
| C-4 AI 兜底 | 漏斗末端 | 點選為主；自由打字且未命中才送 AI（MiniMax）；送出前告知第三方；伺服端不留文字；每日總額度 | 不做對話歷史、不做個人判定、不留 log | 見 §4 | 不做站內聊天機器人 | P0-4 Worker 正式資源；額度數字；供應商條款揭露文字 |
| C-5 人的連結 | 社團目錄 | 依地區×需求推薦地方社團（LINE／FB／Reddit） | 不做站內配對、佈告欄、私訊 | 無（外連） | 不做會員 | 需求分類與地區對照表 |
| C-6 量測 | 全站 | Cloudflare Web Analytics（無 cookie）＋既有 GA4 同意制並用 | 敏感頁不做個人層級量測；未同意不載入 GA4 | CWA beacon；GA4 同意後 | 不做 session replay | P0-3 人工前置；敏感頁排除清單（SPEC §1.5） |
| C-7 理想情境 | 漏斗出口 | 找房→搜房工具；找工作→工作類型小測驗→平台總表；想去哪／過什麼生活→建議；買車→分流＋地方群組 | 出口不得跳過安全出口 | 無 | — | 小測驗題目與「建議」內容待設計 |

## 2. 不可協商邊界（承自 SDD §1.1，對本專題的具體化）

1. 釐清器任何一層都不得提供個人簽證資格、法律、醫療、稅務判定；只能導向站內頁面、官方出口與專業名冊。
2. 緊急安全出口（立即危險、剛匯款、威脅／扣證件、簽證到期）常駐在漏斗之上，任何狀態下一鍵可達。
3. 零打字優先：漏斗前三層全部是 chips／單選；打字框只在最後一層出現，且先顯示站內搜尋結果。
4. 無 JavaScript 時漏斗退化為靜態 `<details>` 分流（沿用現有 `.route-guide` 模式），內容頁連結完整可用。
5. 不新增框架、不新增建置步驟、不新增第三方腳本（CWA 例外，須登錄 SDD §2 與 SPEC §3）。
6. 站長服務招攬不進漏斗；贊助只留 About。
7. 商業關係不得影響出口順序；社團入口須登錄 `third-party-register.json`。

## 3. 漏斗骨架（草案，待站長審核）

### 3.1 層級

| 層 | 問題 | 選項（chips） | 備註 |
|---|---|---|---|
| 0 | （常駐）如果事情很急 | 4 個安全出口 | 沿用 `#support-hub` |
| 1 | 你現在在哪一步？ | 出發前／已在澳洲／準備離開 | 決定後續題目集 |
| 2（只在「出發前」） | 你拿哪本護照？ | 台灣（417）／中國大陸（462）／其他 | 462 → 站內釐清續行，簽證細節連 `lang/en/visa/`；其他 → `lang/` 語言入口 |
| 3 | 你最想先解決哪件事？ | 找房／找工作／簽證與集簽／錢、稅與換匯／想去哪、過什麼生活／買車／安全與健康／離澳收尾／先玩一次（模擬器）／找人聊 | 依第 1 層縮減選項 |
| 4 | 出口 | 依 C-7 對照表直達工具或頁面錨點 | 每個出口附「找人聊」的地區社團入口（C-5） |
| 5（兜底） | 沒有你的情況？ | 站內搜尋結果 → 仍沒有 → 自由打字送 AI（§4） | 打字前明示第三方 |

### 3.2 既有首頁區塊去向（C-1 要求每塊都有去向）

| 現有區塊 | 去向 |
|---|---|
| `#support-hub` 安全出口 | 第 0 層常駐 |
| `#self-assessment` 自我評估 | 第 3 層「想去哪、過什麼生活」出口 → `why.html`；「先玩一次」→ `simulator.html` |
| `#common-problems` 12 張旅程問題卡＋3 張直接解法 | 拆成第 3 層選項與第 4 層出口對照表；卡片文案回收為出口說明 |
| `#communities` 社團目錄 | 第 4 層每個出口的「找人聊」＋獨立入口「找人聊」；依地區×需求篩選（C-5） |
| `#games` 遊戲區 | 第 3 層「先玩一次」出口 |
| `#search` 全站搜尋 | 第 5 層第一段；header 入口保留 |
| `.route-guide` 兩題引導 | 被漏斗取代；其 no-JS `<details>` 寫法沿用為 fallback |
| `#journey-resume`、`#saved-pages` | 漏斗下方次要區，維持既有 localStorage 行為 |
| `#source-model`、`#principles` | 首頁保留精簡版一段，完整內容在 `about.html` |
| 資料來源模型、回報修正入口 | 頁尾與回饋列 |

### 3.3 出口對照表（C-7 的具體化，待補齊）

| 第 3 層選項 | 出發前 | 已在澳洲 | 準備離開 |
|---|---|---|---|
| 找房 | `housing.html#housing-search-tool`（短住） | 同左（合租／整租） | — |
| 找工作 | 工作類型小測驗（待設計）→ `work.html` 平台總表 | 同左＋採收月曆 | — |
| 簽證與集簽 | `visa.html`（462 → `lang/en/visa/`） | `visa.html#postcode-tool` | `pr.html` |
| 錢、稅與換匯 | `cost.html#save-calc`、`cost.html#exchange` | 同左 | `leave.html#dasp-calc` |
| 想去哪、過什麼生活 | `why.html` 快思 → 建議（待設計） | 同左 | — |
| 買車 | `cost.html` 買車段落 → 地方群組 | 同左 | `market.html` |
| 安全與健康 | `health.html`、`scam.html` | 同左 | — |
| 離澳收尾 | — | — | `leave.html#leave-checklist-tool`、`market.html` |

## 4. AI 兜底規則（C-4）

- **觸發條件**：第 5 層且站內搜尋零結果或使用者明確按「還是沒有」；其他任何層不得出現 AI。
- **送出前揭露**（必顯示、可關閉）：「你的問題會送到第三方模型（MiniMax）產生回覆；本站伺服器不保存問題文字，
  但供應商可能依其條款處理。請不要輸入姓名、護照、帳號或他人資料。」措辭最後以站長審核為準。
- **送出內容**：只送問題文字＋固定的站內頁面索引摘要（讓模型只能引用站內連結）；不送 cookie、識別碼、頁面歷史、地理位置。
- **後端**：新 Worker 路由（建議 `POST /api/assist`，需登記於 SDD §3.1），受 P0-4 gate；
  Turnstile 驗證；限流鍵用 `HMAC(CF-Connecting-IP)`（不得重蹈住宿搜尋全域常數鍵的問題）；
  每日總額度計數只存一個日期 counter；超額回固定訊息並顯示搜尋與社團目錄；不寫 D1、不留 request log。
- **回覆約束**：模型系統提示限制只能回站內頁面連結與一句導引；回覆經白名單過濾，只允許同站 URL；
  含簽證資格、法律、醫療、稅務判定字眼的回覆改為固定的官方出口文案。
- **敏感輸入**：問題含自傷、暴力、剛匯款等關鍵詞時不送 AI，直接顯示安全出口。
- **驗收**：origin 留空時零 request；超額 fail closed；回覆不含外站 URL；no-JS 不顯示打字框。

## 5. 社團目錄（C-5）

- 資料：既有首頁 9 個公開入口（1 LINE＋8 Reddit）為種子；擴充時每筆登錄 `third-party-register.json`
  並附查核日期；FB 只做平台搜尋連結，不列具體社團除非通過 SDD §3.2 上架欄位。
- 推薦邏輯：地區（州別／城市）×需求（找房、找工作、生活交流、買賣）→ 排序依 SDD §3.2 自然排序，
  不依付費；無合適入口時顯示平台搜尋。
- 邊界：不做站內配對、佈告欄、私訊、會員；不作求職／租屋查核；不作緊急支援或專業轉介。

## 6. 量測（C-6）

- Cloudflare Web Analytics：無 cookie、不需同意；啟用時登錄 SDD §2 外部依賴與 SPEC §3；beacon token 是公開值，可進 repo。
- GA4：維持 Basic Consent；填 ID 前完成敏感頁排除清單（SPEC §1.5）；D+ 既有 7 類別保留。
- 釐清器專屬指標（先設計，後量測）：各層完成率、出口點擊、AI 觸發率、超額次數；
  以 D+ 白名單 key 或 CWA 事件實作，不建立個人層級資料。

## 7. 驗收條件（草案）

1. 桌機、390px、no-JS 三種情境都能從首頁到達任一出口，且安全出口在每層可達。
2. 漏斗前三層零打字；鍵盤可操作；`prefers-reduced-motion` 無動畫。
3. `scripts/check.ps1` 通過，並新增「每個既有區塊去向」的斷言（§3.2 表每列的目標錨點存在）。
4. AI 相關：§4 驗收四項。
5. 自願任務測試：30 秒找路成功率不低於改版前基線（基線由量測上線後建立）。

## 8. 待站長決定的問題

1. `market.html` 是否進全站 nav（`ROADMAP.md` §3 第一項）。
2. AI 每日總額度數字與超額文案。
3. MiniMax 資料處理條款的揭露文字；是否需要在 About 隱私段落新增一節。
4. 工作類型小測驗的題目與結果分類。
5. 「想去哪、過什麼生活」的建議內容來源（現有 `why.html` 快思分面是否足夠）。
6. 12 張旅程問題卡的文案是否全數回收，或保留部分作為第 4 層說明。
